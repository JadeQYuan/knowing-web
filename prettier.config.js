module.exports = {
    // Specify the line length that the printer will wrap on. // 80
    printWidth: 100,
    // Specify the number of spaces per indentation-level. // 2
    tabWidth: 4,
    // Use single quotes instead of double quotes. // false
    singleQuote: false,
    // Print trailing commas wherever possible when multi-line. // none
    trailingComma: "all",
    // End of Line // auto
    endOfLine: "lf",

    // Indent lines with tabs instead of spaces.
    // useTabs: false,
    // Print semicolons at the ends of statements.
    // semi: true,
    // Print spaces between brackets in object literals.
    // bracketSpacing: true,
    // Include parentheses around a sole arrow function parameter.
    // alwaysParens: "avoid",
    // Whether or not to indent the code inside <script> and <style> tags in Vue files.
    // vueIndentScriptAndStyle: false,
};
